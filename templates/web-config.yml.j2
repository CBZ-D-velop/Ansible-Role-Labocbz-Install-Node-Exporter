##############################
#                            #
# Ansible / Tower managed on #
#                            #
##############################

{% if install_node_exporter__ssl | default(false) %}
tls_server_config:
  cert_file: "{{ install_node_exporter__ssl_crt }}"
  key_file: "{{ install_node_exporter__ssl_key }}"
{% endif %}

{% if install_node_exporter__basic_auth | default(false) %}
basic_auth_users:
  {{ install_node_exporter__basic_auth_login }}: "{{ install_node_exporter__basic_auth_password_hash }}"
{% endif %}

